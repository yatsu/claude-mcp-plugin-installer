{
  "$schema": "https://www.schemastore.org/claude-code-plugin-manifest.json",
  "name": "github-mcp",
  "description": "GitHub remote MCP server, authenticated with a PAT stored in the OS keychain (Variant A)",
  "version": "0.1.0",
  "author": {
    "name": "Masaki Yatsu"
  },
  "userConfig": {
    "token": {
      "type": "string",
      "title": "GitHub personal access token",
      "description": "Create at github.com > Settings > Developer settings. Grant only the scopes you need (e.g. repository read access). Stored in the macOS Keychain, never in a config file.",
      "sensitive": true,
      "required": true
    }
  }
}
