{
  "$schema": "https://www.schemastore.org/claude-code-plugin-manifest.json",
  "name": "context7-mcp",
  "description": "Context7 remote MCP server, authenticated with an API key stored in the OS keychain (Variant A)",
  "version": "0.1.0",
  "author": {
    "name": "Masaki Yatsu"
  },
  "userConfig": {
    "token": {
      "type": "string",
      "title": "Context7 API key",
      "description": "Create at context7.com/dashboard. Keys look like ctx7sk-... Stored in the macOS Keychain, never in a config file.",
      "sensitive": true,
      "required": true
    }
  }
}
