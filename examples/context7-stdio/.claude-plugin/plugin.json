{
  "$schema": "https://www.schemastore.org/claude-code-plugin-manifest.json",
  "name": "context7-stdio-mcp",
  "description": "Context7 local stdio MCP server, API key passed through env from the OS keychain (Variant C)",
  "version": "0.1.0",
  "author": {
    "name": "Masaki Yatsu"
  },
  "userConfig": {
    "token": {
      "type": "string",
      "title": "Context7 API key",
      "description": "Create at context7.com/dashboard. Keys look like ctx7sk-... Passed to the server as CONTEXT7_API_KEY through the environment — never as a CLI argument.",
      "sensitive": true,
      "required": true
    }
  }
}
